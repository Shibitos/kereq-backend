CREATE TABLE DICTIONARIES (
	DICT_ID BIGINT PRIMARY KEY,
	DICT_AUDIT_CD TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	DICT_AUDIT_CU BIGINT,
	DICT_AUDIT_MD TIMESTAMP,
	DICT_AUDIT_MU BIGINT,
	DICT_AUDIT_RD TIMESTAMP,
	DICT_VERSION BIGINT NOT NULL DEFAULT 0,
	DICT_CODE VARCHAR(25) NOT NULL
);

CREATE SEQUENCE SEQ_DICT_ID START 50 INCREMENT 50;
CREATE UNIQUE INDEX IDX_UQ_DICT_CODE ON DICTIONARIES(UPPER(DICT_CODE));

CREATE TABLE DICTIONARY_ITEMS (
    DICT_ITEM_ID BIGINT PRIMARY KEY,
	DICT_ITEM_AUDIT_CD TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	DICT_ITEM_AUDIT_CU BIGINT,
	DICT_ITEM_AUDIT_MD TIMESTAMP,
	DICT_ITEM_AUDIT_MU BIGINT,
	DICT_ITEM_AUDIT_RD TIMESTAMP,
	DICT_ITEM_VERSION BIGINT NOT NULL DEFAULT 0,
	DICT_ITEM_DICT_ID BIGINT REFERENCES DICTIONARIES(DICT_ID) NOT NULL,
	DICT_ITEM_CODE VARCHAR(25) NOT NULL,
	DICT_ITEM_VALUE VARCHAR(50) NOT NULL
);

CREATE SEQUENCE SEQ_DICT_ITEM_ID START 50 INCREMENT 50;
CREATE UNIQUE INDEX IDX_UQ_DICT_ITEM_CODE ON DICTIONARY_ITEMS(UPPER(DICT_ITEM_CODE));
